# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# wrangler's local state
.wrangler/
# the JSON the export script writes; rebuilt from ../data on every build
src/data/*.json
# the tree picture the export script draws (website step 22); rebuilt the same way
public/tree.svg
# logs and macOS
npm-debug.log*
.DS_Store
