{
  "name": "belt-equation-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "export": "python3 ../scripts/export.py",
    "predev": "python3 ../scripts/export.py",
    "dev": "astro dev",
    "prebuild": "python3 ../scripts/export.py",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
