# Scripts

This folder will hold the code that turns the tree into headline numbers: it runs the tree many thousands of times with random outcomes, respecting which breakthroughs depend on which, and reports how often each end state is reached. Empty until the tree exists.
