# Changelog

Every change to the tree, the definitions, or the headline numbers, with absolute dates. Newest first.

## 2026-09-08

- First brainstorm session (roadmap step 4, window and biology): seven window nodes and nine biology nodes added to data/, criterion first for each, two long shots with mechanism and breaking point, six leaves. The tree has 17 nodes and validates. No probabilities. Definitions amended: the deadline is the scenario axis, not a node of its own. Idea recorded under "Later, not now": a review method that brings in other models for fresh critique.
- Roadmap step 5 done: scripts/compute.py written against the schema. Standard library only. Commands: validate (all eight schema rules), shape (counts, leaves, tiers), run (the tree played out per scenario, tiers and Contact Clause reported), compare (one choice group at a time against the current plan). Refuses to report numbers until every open world node has probabilities. Checked on the real data and on a small test tree whose results match hand arithmetic.
- Second founding rule added to docs/definitions.md, stated by John: plausible all the way to the edge of the impossible. A long-shot node must name its mechanism and state plainly where current physics or engineering would have to give way. Carried into docs/node-schema.md as a review test: a long-shot node without both fails the same way a node without a resolution criterion does.
- data/scenarios.toml baseline moved to the refined window, 2071 plus or minus 8, no longer provisional.
- Ground rule added to docs/roadmap.md, ruled by John: pull requests are merged with the gh command-line tool, not the GitHub website.
- Roadmap step 2 decided: John accepted the node schema with two amendments Claude made on reflection after stating confidence and alternatives for each choice. Choice points now default to a named current-plan option instead of "not taken"; "either route" logic moves from the tier file onto nodes (depends_on_any), so the habitat hedge becomes one named biology node; the compute script will refuse probabilities that fall as the window lengthens. Known simplifications recorded in docs/node-schema.md. tiers.toml loses requires_any.
- New ground rule in docs/roadmap.md, ruled by John: every design choice put to him carries a confidence level, whether it is standard practice, and the strongest alternative, before he is asked to accept it.
- Roadmap step 2 drafted, awaiting John's acceptance: docs/node-schema.md (the node record; TOML, one file per factor; tiers as required-node sets; choice points forced, not sampled; probabilities per scenario and conditional on dependencies), data/scenarios.toml, data/tiers.toml with empty node lists, and the first real node in data/L-launch.toml (a fully reusable heavy launcher flying 100 orbital missions in a year).
- Window refined from the health record to 2071 plus or minus 8 (baseline scenario moved from 2070 to 2071). A leverage branch (AI capability as a multiplier on John's own participation) added under the access factor. A private, git-ignored transcript of the scene that inspires the project added under docs/private/.
- Roadmap step 1 done: the kickoff outline reconciled against definitions.md. The outline now carries a note listing what the definitions superseded (three branches to eight factors, location-agnostic tiers, four scenarios, per-scenario re-runs, the node schema) and a starting map of its branch seeds onto the eight factors. The public-forecasting reference from the outline is carried into roadmap step 9. README now points to the roadmap as the plan. Definitions unchanged.
- Roadmap reviewed with Claude, all six changes approved by John: the node brainstorm runs as three grouped sessions; the compute script moves into Phase 2 in parallel with the brainstorm (now step 5); the window refinement (step 3) is marked non-blocking and sits after the brainstorm in the TimeAssembler task order; the habitat branch is folded into the brainstorm sessions instead of standing alone; a decision-comparison run at the six-year service fork is added as step 11; the first essay will cross-reference the Calibration Problem manuscript. No definitions changed.
- Project created and named The Belt Equation.
- Kickoff outline written (docs/2026-09-08-kickoff.md).
- Definitions document started (docs/definitions.md) and, the same evening, every founding decision made: the question wording, the access tiers A0 to A3 with A2 as the headline, the system tiers 1 to 4 with Tier 3 as the "Belt exists" bar and habitats included, the eight factors frozen, the window placeholder at 2070 with four longevity scenarios including escape velocity, the founding rule that long shots stay on the board, the Contact Clause beside the equation, the access branch kept provisional until pipeline graduation with choice-point nodes, and the public-but-under-promoted posture with a quarterly scan and January annual review.
- Roadmap written (docs/roadmap.md) for the work before the pipeline resumes.
- No nodes, no probabilities yet.
